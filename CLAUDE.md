# NeuroCoreLab — project context (auto-loaded by Claude Code)

This repo is a **NeuroCoreLab** brain: a file-based context layer for AI coding agents.
Concept → `README.md`. Skill set → `docs/skills.md`. Neuron schema → `docs/neuron-schema.md`.

## On every session
- **If this instance isn't configured yet** (no `config/identity.md` or `INDEX.md`): run
  **`/ncl-init`** to set it up (identity, repos, accounts, connectors). Do this first.
- **If it is configured:** the cortex below is your always-loaded working memory. Open the
  neuron files it points to for detail. Prefer answering from the brain (**`/ncl-recall`**)
  before asking the user to re-explain.
- **At the end of substantive work:** consolidate with **`/ncl-sync`** (capture decisions,
  refresh the cortex, stamped by the active account).

## Skills
`/ncl-init` · `/ncl-accounts` · `/ncl-sync` · `/ncl-recall` · `/ncl-handoff` · `/ncl-decision`
· `/ncl-project` · `/ncl-doctor` — full catalog in `docs/skills.md`.

## Cortex
<!-- Created by /ncl-init from INDEX.template.md. Before init this include may be empty. -->
@INDEX.md
